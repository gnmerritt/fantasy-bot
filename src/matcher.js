/**
 * Decorates player objects with a draft API id
 */
(function(document, window, undefined) {
 'use strict';

window.IdMatcher = function(playerEstimates) {

    return {
        match: function() {}
    }
};

})(document, window);
