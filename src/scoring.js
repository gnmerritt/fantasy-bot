var SCORING = {};

/**
 * Scoring system manager, converts a set of projected feats into a
 * points total depending on the system
 */
scoring = function(document, window, undefined) {
 'use strict';

}(document, window);
